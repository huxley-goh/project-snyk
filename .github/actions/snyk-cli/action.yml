name: "Download Snyk and Initialize"
description: "Download Snyk and Initialize"
inputs:
  snyk-token:
    description: 'Snyk authentication token'
    required: true
runs:
  using: "composite"
  steps:
    - name: "Donwload, Install and Initialize Snyk"
      shell: bash
      run: |
        curl https://github.com/snyk/snyk-to-html/releases/latest/download/snyk-to-html-linux -o snyk-to-html
        chmod +x ./snyk-to-html
        mv ./snyk-to-html /usr/local/bin/
        curl https://static.snyk.io/cli/latest/snyk-linux -o snyk
        chmod +x ./snyk
        mv ./snyk /usr/local/bin/
        snyk auth $SNYK_TOKEN
      env:
        SNYK_TOKEN: ${{ inputs.snyk-token }}

